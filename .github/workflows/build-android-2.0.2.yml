name: Legacy Nexa Android 2.0.7 build (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow retired
        run: |
          echo "This workflow is intentionally retired."
          echo "Nexa production Android releases are built by release-android-api36.yml."
          echo "Current release baseline: 2.0.8 / versionCode 101 / Android API 36."
